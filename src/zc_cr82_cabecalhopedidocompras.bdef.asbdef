projection;
strict ( 2 );
use draft;

define behavior for ZC_CR82_CABECALHOPEDIDOCOMPRAS //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use association _Itens { create; with draft; }
}

define behavior for ZC_CR82_ITENSPEDIDOCOMPRAS //alias <alias_name>
{
  use update;
  use delete;

  use association _Cabecalho { with draft; }
}